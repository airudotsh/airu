{
  "name": "airu",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "chat": "bun run --filter 'airu' chat",
    "prepublishOnly": "bun run build && bun run typecheck && bun test"
  },
  "devDependencies": {
    "airu": "workspace:*",
    "@airu/core": "workspace:*",
    "@airu/plugins": "workspace:*",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
